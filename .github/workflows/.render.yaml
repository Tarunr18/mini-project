services:
  - type: web
    name: my-flask-app
    env: docker
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    stratCommand: "gunicorn app:app"
    autoDeploy: false
